import fetch from "node-fetch";

async function run() {
  try {
    // We will use the user's real proxy headers but we don't have the token.
    console.log("No token available to test metric");
  } catch (e) {
  }
}
run();
